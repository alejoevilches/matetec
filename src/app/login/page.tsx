import LoginForm from "./components/LoginForm"

export default function LoginView(){
  return (
    <section className="flex h-screen content-center justify-center">
      <LoginForm />
    </section>
  )
}